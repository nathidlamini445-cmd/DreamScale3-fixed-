'use client'

/**
 * Auto-open on load disabled — Pro welcome opens from the bell next to Settings.
 * Kept as empty export for pages that still import it.
 */
export function ProWelcomeHost() {
  return null
}
